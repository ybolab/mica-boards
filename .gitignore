_out/
tmp/
.tmp/

# The source dependencies, fetched at their pins by tools/deps.sh out of the
# registry's mica-source artifacts of ybolab/mica-build-env and ybolab/mica-boot
# (deps/sources/); never edited in place, never committed here.
/build-env/
/boot/

# The signing workspace the kernel builds embed their trust certificate from
# (a copy of, or a symlink to, the assembly's meta/); private.
/meta/
